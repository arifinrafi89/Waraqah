import 'package:flutter/material.dart';

import 'p2p_marketplace_page.dart';

/// Full P2P marketplace tab, built to match the current feature architecture.
class P2pPage extends StatelessWidget {
  const P2pPage({super.key});

  @override
  Widget build(BuildContext context) => const P2pMarketplacePage();
}
